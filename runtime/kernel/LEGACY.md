# Legacy gw-kernel

This crate is superseded by `global-workspace-runtime-rs/`.

The kernel's useful types (event variants, replay, reducer patterns) have been
migrated to the authoritative Rust workspace. This directory remains as a
reference only and will be removed in a future release.

See `global-workspace-runtime-rs/` for the active Rust codebase.
